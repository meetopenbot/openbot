# Browser Agent

This agent browses the web, extracts data, and interacts with pages.

## Requirements

This agent requires the `browser-tools` plugin.

### Installation

To install the required plugin, run:

```bash
openbot plugin install meetopenbot/plugin-browser-tools
```

## Configuration

The agent is configured to use a headless browser and store user data in `~/.openbot/browser-data`.
